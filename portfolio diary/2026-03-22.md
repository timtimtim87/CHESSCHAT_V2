# 2026-03-22 - Template-Driven Frontend Rebuild (UI Pass 1)

## Goals for today
- Rebuild app frontend visuals using `screen_templates` HTML and PNG references as source-of-truth.
- Preserve existing backend/API/WebSocket behavior while upgrading UI layout and design system.
- Add non-functional UI stub routes for features not yet implemented.
- Clarify split-host boundary for runtime app vs static landing.

## What was implemented
- Added shared app chrome component:
  - `app/frontend/src/components/AppChrome.jsx`
  - Includes top navigation + side navigation pattern for authenticated app routes.
- Reworked styling foundation:
  - `app/frontend/src/styles.css`
  - Added Obsidian-Slate-inspired tokens, component primitives, and page shells.
- Updated route screens:
  - `LandingPage.jsx` redesigned to match template tone while keeping Hosted UI auth actions.
  - `LobbyPage.jsx` redesigned with existing username setup + challenge flow preserved.
  - `ProfilePage.jsx` redesigned with existing profile fetch and delete-account behavior preserved.
- Added new UI-only stub routes:
  - `FriendsPage.jsx` at `/friends`
  - `HistoryPage.jsx` at `/history`
  - Each includes explicit `API_MISSING` tags for non-implemented controls.
- Updated app routing:
  - `App.jsx` now includes protected routes for `/friends` and `/history`.

## Architecture clarification captured
- Confirmed split-host direction:
  - `chess-chat.com` static edge landing only.
  - `app.chess-chat.com` containerized app surfaces (lobby/profile/history/friends/room/auth callback).
- This was documented in both UI and strategy docs.

## Validation run
- `npm --prefix app/frontend run build` -> pass.
- `npm --prefix app/frontend run test` -> all suites reported pass in output; process did not terminate cleanly in this local environment after reporting.

## Follow-up roadmap (next slice)
1. Wire friends list/request actions to backend endpoints and WS events.
2. Wire history filters/export/review to backend data APIs.
3. Expand profile settings (verification/privacy/linked account) from UI stub to functional workflow.
4. Add visual regression snapshots for key routes (`/`, `/lobby`, `/profile`, `/friends`, `/history`, `/room/:code`).
