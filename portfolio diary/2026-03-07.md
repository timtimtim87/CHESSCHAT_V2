# 2026-03-07

## What changed
- Added a new cloud architecture draft artifact with three Mermaid diagrams:
  - `diagrams/chesschat-cloud-architecture-draft.md`
  - Diagram 1: system context
  - Diagram 2: AWS runtime topology (network + placement)
  - Diagram 3: delivery/IAM trust and deploy flows
- Updated `DOCS/diagram-guide.md` with a "Current Draft Pack (2026-03-07)" pointer and current baseline values.

## How it was done
- Reviewed source-of-truth docs and Terraform wiring:
  - `DOCS/PORTFOLIO_BUILD_PLAYBOOK.md`
  - `infra/RESOURCE_REGISTRY.md`
  - `terraform/backend.tf`, `terraform/main.tf`, `terraform/README.md`
  - `terraform/modules/*/main.tf` and outputs
  - `terraform/environments/dev/terraform.tfvars`
- Mapped each provisioned service and relationship into labeled Mermaid node/edge flows:
  - Ingress: Route53 -> ALB -> ECS
  - Identity: Cognito auth flows
  - Data: ECS -> DynamoDB/Redis/Secrets Manager
  - Observability: CloudWatch alarms + Budgets -> SNS
  - Delivery: GitHub Actions OIDC -> deploy role -> ECR/ECS/Cognito

## Why decisions were made
- Produced a 3-diagram set to match solutions-architect interview storytelling:
  - Context view for audience orientation
  - Runtime network/deployment view for infrastructure depth
  - IAM trust/deploy view for security and delivery maturity
- Added explicit tradeoff callouts directly in diagrams:
  - Single NAT gateway (cost optimization vs egress resilience)
  - ECS desired count 1 (portfolio baseline)
  - Future hardening path (WAF, per-AZ NAT, stronger service baseline)
- Kept all statements grounded to Terraform + registry state to avoid drift between docs and deployed reality.

## Additional update (Draw.io native implementation)
- Implemented native Draw.io source files for direct editing in VS Code Draw.io extension and diagrams.net:
  - `diagrams/chesschat-system-context.drawio`
  - `diagrams/chesschat-runtime-topology.drawio`
  - `diagrams/chesschat-delivery-iam.drawio`
- Updated `diagrams/README.md` to document:
  - repository rule favoring `.drawio` for diff-friendly reviews,
  - extension-based edit workflow (`Reopen Editor With...`),
  - optional export path to `.drawio.svg` / `.drawio.png`.
- Validation:
  - XML parse check passed on all 3 files (`xmllint --noout ...`).

## Additional update (AWS icon-library copies)
- Added separate Draw.io AWS icon versions (kept original flowchart files unchanged):
  - `diagrams/chesschat-system-context-aws-icons.drawio`
  - `diagrams/chesschat-runtime-topology-aws-icons.drawio`
  - `diagrams/chesschat-delivery-iam-aws-icons.drawio`
- Implementation detail:
  - Used Draw.io AWS4 icon styles (`mxgraph.aws4.resourceIcon` with service `resIcon` mappings) for core services.
  - Kept trust-boundary/grouping and edge labels aligned to Terraform + registry wiring.
- Validation:
  - XML parse check passed for all AWS-icon files (`xmllint --noout ...`).
