# 2026-03-10

## What changed
- Formalized cross-agent continuity rules so Codex and Claude Code can alternate sessions without losing context.
- Updated project-level agent policy to require end-of-session handoff artifacts, required doc updates, and PR creation.
- Updated docs index to treat handover files as authoritative context for the next session.

## How it was done
- Updated:
  - `AGENTS.md`
  - `DOCS/README.md`
- Added explicit workflow language for:
  - one active agent at a time,
  - mandatory handover updates,
  - required documentation touchpoints,
  - commit/push/PR closure per substantial session.

## Why decisions were made
- Commit authorship uses Tim's GitHub identity for both agents, so continuity must be enforced by workflow artifacts and documentation quality rather than git author names.
- A strict handover + PR cadence prevents context drift between Codex and Claude sessions and keeps work auditable for portfolio/interview storytelling.

## What changed (Session 2)
- Updated `AGENTS.md` to enforce a mandatory session-start git sync rule for both Claude Code and Codex:
  - fetch latest `origin/main`
  - fast-forward `main`
  - create a fresh `codex/<topic>` branch
  - do not continue from branches behind `main`
- Added the same requirement to the consistency checklist so PR prep explicitly verifies this happened.

## How it was done (Session 2)
- Added a new "Session Start Sync Rule (Mandatory)" section in `AGENTS.md` with exact command sequence:
  - `git fetch origin`
  - `git checkout main`
  - `git pull --ff-only origin main`
  - `git checkout -b codex/<topic>`
- Added a top-level rule under "Rules for both agents" making this startup flow the default behavior every session.

## Why decisions were made (Session 2)
- Prevents stale-branch starts across both agents, which was causing session drift from `main`.
- Makes startup behavior deterministic and auditable in PR review.

## What changed (Session 3 - Stage 3 app auth bootstrap)
- Added token-refresh support on app host auth context:
  - `getValidToken()` now checks cookie token age with a 60-second buffer.
  - When stale, it calls Cognito `REFRESH_TOKEN_AUTH` and returns a fresh access token.
- Updated WebSocket client to request token per connect/reconnect via async callback (`getToken`) instead of a static constructor token.
- Wired room page socket creation to pass `getValidToken`.
- Added frontend tests for:
  - token refresh trigger path
  - socket reconnect token retrieval behavior
  - unauthorized close stops reconnect loop

## How it was done (Session 3)
- Updated frontend auth/socket integration:
  - `app/frontend/src/context/AuthContext.jsx`
  - `app/frontend/src/services/socket.js`
  - `app/frontend/src/pages/RoomPage.jsx`
- Updated frontend tests:
  - `app/frontend/src/context/AuthContext.test.jsx`
  - `app/frontend/src/pages/RoomPage.test.jsx`
  - `app/frontend/src/services/socket.test.js` (new)

## Validation evidence (Session 3)
- `npm --prefix app/frontend run test` (pass)
- `npm --prefix app/frontend run build` (pass)

## What changed (Session 4 - Stage 3 merge + CI unblock closure)
- PR #19 merged to `main` for Stage 3 auth bootstrap reliability.
- Added CI stabilization for `frontend-quality`:
  - Vitest runs in forked pool mode.
  - Coverage run excludes `RoomPage.test.jsx` (temporary workaround for hanging coverage path).
- Wrote post-merge handover for next agent and advanced handover protocol pointer.

## Why decisions were made (Session 4)
- Branch protection required passing checks; frontend coverage hang needed a deterministic unblock.
- Temporary exclusion keeps CI reliable while preserving most frontend coverage signal.

## What changed (Session 5 - Stage 4 room lifecycle ordering)
- Implemented Stage 4 backend teardown ordering correction in websocket lifecycle handling.
- Updated `finalizeRoomTeardown` to perform atomic room teardown + code tombstone write before Chime deletion:
  - removed split calls to `deleteRoom` and `markRoomCodeConsumed`
  - replaced with `teardownRoomAndConsumeCode(roomCode, config.app.roomConsumedTtlSeconds)`
- Kept terminal gate and Chime delete retry/logging behavior unchanged.

## How it was done (Session 5)
- Synced from `origin/main` and created fresh branch `codex/stage-4-room-lifecycle` from `origin/main`.
- Re-applied pending stash `stage-4-pending-handler-change` and verified it only touched:
  - `app/backend/src/websocket/handler.js`
- Updated handover context:
  - Added `DOCS/handovers/2026-03-10-004-codex-to-claude.md`
  - Advanced latest pointer in `DOCS/AGENT_HANDOVER_PROTOCOL.md`

## Validation evidence (Session 5)
- `npm --prefix app/backend run test` (pass, 17/17)
- `npm --prefix app/backend run test:coverage` (pass, 17/17)
- `npm --prefix app/frontend run build` (pass)
- `terraform -chdir=terraform validate` (failed in local environment due provider plugin schema/startup error; no Terraform files changed in this session)

## Why decisions were made (Session 5)
- Stage 4 hard constraint requires tombstone/write-before-Chime-delete and atomic room teardown to prevent lifecycle race windows.
- Used existing `teardownRoomAndConsumeCode` helper to enforce ordering with minimal scope and no protocol drift.

## What changed (Session 6 - Stage 5 username/docs closure)
- Completed Stage 5 as docs/policy closure after Stage 4 merge.
- Verified username regex policy consistency across required surfaces:
  - backend profile username validation,
  - frontend lobby username form,
  - static-auth signup flow.
- Confirmed required ADR set already exists for Stage 5 scope:
  - split-host architecture,
  - JS-managed cookie v1 risk acceptance,
  - phone-call room lifecycle,
  - Apple Sign In defer rationale.
- Updated:
  - `DOCS/PORTFOLIO_BUILD_PLAYBOOK.md`
  - `DOCS/UI_DESIGN_GUIDE.md`
  - `infra/RESOURCE_REGISTRY.md`
  - `DOCS/AGENT_HANDOVER_PROTOCOL.md`
  - `DOCS/handovers/2026-03-10-005-codex-to-claude.md`

## How it was done (Session 6)
- Synced with `origin/main` after PR #21 merge and created fresh branch `codex/stage-5-username-docs`.
- Ran targeted source/doc inspection using `rg` + file reads to confirm policy/ADR coverage.
- Applied documentation updates only (no runtime code changes, no Terraform changes).

## Validation evidence (Session 6)
- Docs-only stage closure.
- Validation method: source inspection confirmation for regex and ADR artifacts (no build/apply required for this scope).

## Why decisions were made (Session 6)
- Stage 5 objective was governance/policy closure rather than new runtime implementation.
- Avoided unnecessary code churn by documenting and verifying existing correct behavior.
